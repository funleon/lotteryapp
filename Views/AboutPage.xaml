<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="lotteryapp.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:lotteryapp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="40">
        <StackPanel Spacing="16" MaxWidth="600" HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBlock Text="關於 (About)" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="24">
                <Grid ColumnSpacing="16" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 應用系統 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="應用系統:" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Lottery Machine" />

                    <!-- 版本 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="版本:" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="1.0" />

                    <!-- 日期 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="日期:" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="2026.2.22" />

                    <!-- 作業系統 -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="作業系統:" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="Windows 11 X64" />

                    <!-- 作者 -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="作者:" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="Leon Shih" />

                    <!-- 連絡 -->
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="連絡:" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="funleons@gmail.com" />

                    <!-- 軟體授權 -->
                    <TextBlock Grid.Row="6" Grid.Column="0" Text="軟體授權:" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Row="6" Grid.Column="1" Text="MIT" />
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</Page>
